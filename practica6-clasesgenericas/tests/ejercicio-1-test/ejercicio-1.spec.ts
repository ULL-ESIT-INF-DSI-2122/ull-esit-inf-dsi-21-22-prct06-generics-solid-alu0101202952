import 'mocha';
import {expect} from 'chai';
import {Fighter} from '../../src/ejercicio-1/fighter';

describe('Fighter', () => {
  it('Fighter.getNombreLuchador() returns nombre del luchador1', () => {
      return expect().to.be.equal('Flareon');
  })
  
});